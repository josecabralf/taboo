# In-project SDK: carries the setup hooks (setup-base, setup-project) that
# provision the toolchain the make targets need: Go tooling, golangci-lint, and
# (in the dev workshop only) the Material for MkDocs venv for `make docs-serve`.
name: taboo
