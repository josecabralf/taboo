# In-project SDK: carries the setup hooks (setup-base, setup-project). Its `docs`
# tunnel slot is declared on this SDK's entry in workshop.yaml, next to the
# matching system plug, so `make docs-serve` is reachable at localhost:8000.
name: taboo
